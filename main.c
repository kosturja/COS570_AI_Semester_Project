#include <stdio.h>

int main(int argc, char *argv)
{
	//Need to change the file so I can commit it
	//Another change mayber
	//This will be the start of my AI semester project
	//Which will be a GO! playing program

	typedef struct{
		int *board;
		int which_player; //value to determine who's turn it is.
		int *moves; // A location to store valid moves of the current board layout
		
	}Board
	printf("Just a simple program that is really a place holder\n");
	return 0;
}
